{
  "name": "doubletick-webhook-server",
  "version": "1.0.0",
  "description": "DoubleTick WhatsApp Cloud API webhook — invisible Unicode token decoder + GCLID attribution",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
